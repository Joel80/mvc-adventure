<?php

/*
declare(strict_types=1);

namespace App\DataFixtures;

use App\Entity\RoomDescriptor;
use App\Entity\ItemDescriptor;
use App\Entity\EventDescriptor;
use App\Entity\ExitDescriptor;
use App\Adventure\AdventureDataSetter;
use Doctrine\Bundle\FixturesBundle\Fixture;
use Doctrine\Bundle\FixturesBundle\FixtureGroupInterface;
use Doctrine\Persistence\ObjectManager;

class GameItemFixtures extends Fixture implements FixtureGroupInterface
{
    public static function getGroups(): array
    {
        return ['game'];
    }

    public function load(ObjectManager $manager)
    { */
        /* $itemDescriptor = new ItemDescriptor();
        $itemDescriptor->setDescription("An ID card");
        $itemDescriptor->setName("ID card");
        $itemDescriptor->setItemId("idCard");
        $itemDescriptor->setPlacementDescription("An ID card lies in the debris on a desk.");
        $itemDescriptor->setRoomIndex("roomOne");
        $manager->persist($itemDescriptor);

        $itemDescriptor = new ItemDescriptor();
        $itemDescriptor->setDescription("An iron bar");
        $itemDescriptor->setName("Iron bar");
        $itemDescriptor->setItemId("ironBar");
        $itemDescriptor->setPlacementDescription("An iron bar stands in one corner");
        $itemDescriptor->setRoomIndex("roomTwo");
        $manager->persist($itemDescriptor);

        $itemDescriptor = new ItemDescriptor();
        $itemDescriptor->setDescription("A broken calculator");
        $itemDescriptor->setName("Broken calculator - the seven seems to be missing");
        $itemDescriptor->setItemId("calculator");
        $itemDescriptor->setPlacementDescription("A broken calculator lies on the floor");
        $itemDescriptor->setRoomIndex("roomFour");
        $manager->persist($itemDescriptor);

        $itemDescriptor = new ItemDescriptor();
        $itemDescriptor->setDescription("Dirty cloth rag");
        $itemDescriptor->setName("Dirty cloth rag");
        $itemDescriptor->setItemId("dirtyClothRag");
        $itemDescriptor->setPlacementDescription("A dirty cloth rag hangs on the wall next to the southern exit.");
        $itemDescriptor->setRoomIndex("roomFive");
        $manager->persist($itemDescriptor);

        $manager->flush(); */
/*     }
} */
